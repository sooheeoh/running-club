# 파프리카 런클럽 · 로드맵

## 신원(Identity) 모델 — 닉네임 개명 시 데이터 자동 이관

### 배경 / 현재 한계
- 현재 앱은 **계정/로그인 개념이 없음**. 닉네임은 각 기기의 `localStorage(rc_nickname)`에 저장되는 자유 텍스트 라벨.
- 런 문서는 참여자를 **이름 문자열**로 그대로 저장(`participants: ["튼튼", ...]`, `createdBy: "튼튼"`). 사람과 이름을 잇는 안정적 ID가 없음.
- 참여자는 각자가 로그인해서 찍는 게 아니라 **개설자가 이름을 타이핑**해서 넣음 → 앱이 "A=B(개명 전후)"를 알 방법이 없음.
- 익명 인증 UID가 있으나 (1) 런에 저장 안 함 (2) 저장소 초기화·재설치·다른 기기면 바뀜 → 신원으로 못 씀.
- 그래서 **개명해도 과거 데이터가 따라오지 않음**. 개명은 그 기기의 향후 기록 이름만 바꿈.

### 임시 조치 (완료)
- MY 화면의 **"닉네임 변경" 버튼 제거** (개명이 과거 데이터에 반영 안 돼 혼란을 주므로).
  - 최초 설정용 "닉네임 정하기"는 유지.
- 개명이 필요하면 **수동 마이그레이션**으로 처리(runs의 이름 일괄 치환 + 담벼락 표시 별칭).

### 목표 (자동 이관)
1. 각 멤버에게 **고유 안정 ID(userId)** 부여 — 계정 로그인 또는 최소한 재설치에도 유지되는 식별자.
2. 런 문서에 이름 대신 **userId로 저장**, 화면 표시는 `userId → 현재 닉네임` 조회로 렌더.
3. 프로필에서 닉네임을 바꾸면 **전체 화면에 즉시 반영**(과거 기록 포함), 별도 이관 불필요.
4. (선택) 참여자도 각자 self-join으로 본인 userId를 남기게 해 정확도 향상.

### 규모 / 참고
- 데이터 모델·보안 규칙·마이그레이션이 얽힌 **큰 개편**. 도입 시 기존 이름 문자열 데이터를 userId로 백필하는 1회성 마이그레이션 필요.
- 그 전까지는 개명 빈도가 낮으므로 수동 이관으로 충분.
